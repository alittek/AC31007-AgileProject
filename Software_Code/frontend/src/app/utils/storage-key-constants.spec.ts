import { StorageKeyConstants } from './storage-key-constants';

describe('StorageKeyConstants', () => {
  it('should create an instance', () => {
    expect(new StorageKeyConstants()).toBeTruthy();
  });
});
