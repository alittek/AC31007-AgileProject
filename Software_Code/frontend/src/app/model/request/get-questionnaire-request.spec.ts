import { GetQuestionnaireRequest } from './get-questionnaire-request';

describe('GetQuestionnaireRequest', () => {
  it('should create an instance', () => {
    expect(new GetQuestionnaireRequest()).toBeTruthy();
  });
});
