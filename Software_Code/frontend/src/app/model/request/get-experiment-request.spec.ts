import { GetExperimentRequest } from './get-experiment-request';

describe('GetExperimentRequest', () => {
  it('should create an instance', () => {
    expect(new GetExperimentRequest()).toBeTruthy();
  });
});
