import { QuestionnaireDetails } from './questionnaire-details';

describe('QuestionnaireDetails', () => {
  it('should create an instance', () => {
    expect(new QuestionnaireDetails()).toBeTruthy();
  });
});
